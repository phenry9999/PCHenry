#self referencing property of yourself
DropDownMinWidth="{Binding RelativeSource={RelativeSource Self}, Path=ActualWidth}"

#Setting ColSpan = total columns in parent Grid
Grid.ColumnSpan="{Binding ColumnDefinitions.Count, RelativeSource={RelativeSource AncestorType=Grid}}"

#using a converter
IsReadOnlyBinding="{Binding StateCode, Converter={StaticResource NotNullToBooleanConverter}}"

#setting ItemsSource away from current DataContext scope
ItemsSource="{Binding DataContext.AllStateCodes, RelativeSource={RelativeSource AncestorType=telerik:RadWindow}}"
ItemsSource="{Binding DataContext.AllCompanies, RelativeSource={RelativeSource AncestorType=telerik:RadWindow}}"

#Background to system color
Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"


#Set one width based on another x:Name
<Button x:Name="SelectStateTaxExemptionsButton"
        d:Content="Select All for ON"
        Content="{Binding SelectAllTaxExemptionsButtonTitle}"/>
<Button x:Name="SelectAllTaxExemptionsButton"
        Content="Select All"
        Width="{Binding ElementName=SelectAllTaxExemptionsButton, Path=ActualWidth}"/>
